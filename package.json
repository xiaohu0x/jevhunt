{
  "name": "jevhunt",
  "version": "0.2.0",
  "private": true,
  "description": "The directory for software built on Jev — Cloudflare Pages + D1 + Google OAuth",
  "scripts": {
    "build": "node tools/stamp.mjs",
    "catalog:sync": "node tools/sync-projects.mjs",
    "test": "node --test tools/*.test.mjs",
    "dev": "wrangler pages dev",
    "deploy": "node tools/stamp.mjs && wrangler pages deploy",
    "db:migrate:local": "wrangler d1 execute jevhunt-db --local --file=./migrations/0001_init.sql",
    "db:migrate:remote": "wrangler d1 execute jevhunt-db --remote --file=./migrations/0001_init.sql",
    "db:studio": "wrangler d1 execute jevhunt-db --remote --command \"SELECT id, email, name, created_at FROM users ORDER BY created_at DESC LIMIT 20;\""
  },
  "devDependencies": {
    "wrangler": "^4.84.1"
  }
}
